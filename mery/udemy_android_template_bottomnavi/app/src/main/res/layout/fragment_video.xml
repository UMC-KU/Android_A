<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <LinearLayout
        android:id="@+id/album_video_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="15dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/album_video_first"
        android:layout_width="match_parent"
        android:layout_marginTop="20dp"
        android:layout_height="40dp"
        >
        <TextView
            android:id="@+id/song_detail_name"
            android:layout_width="40dp"
            android:layout_height="20dp"
            android:text="최신순"
            android:textSize="14dp"
            android:textColor="@color/gray_color"
            android:textStyle="bold"
            android:layout_marginTop="8dp"
            android:layout_marginStart="320dp"
            app:layout_constraintStart_toStartOf="@id/album_video_first"
            app:layout_constraintTop_toTopOf="@id/album_video_first"
            app:layout_constraintEnd_toStartOf="@id/song_detail_btn"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <ImageView
            android:id="@+id/song_detail_btn"
            android:layout_width="17dp"
            android:layout_height="17dp"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="15dp"
            android:layout_marginTop="14dp"
            android:layout_marginBottom="10dp"
            android:src="@drawable/nugu_btn_down"
            android:textSize="12sp"
            android:textColor="@color/gray_color"
            app:layout_constraintStart_toEndOf="@id/song_detail_name"
            app:layout_constraintTop_toTopOf="@id/album_video_first"
            app:layout_constraintEnd_toEndOf="@id/album_video_first"
            app:layout_constraintBottom_toBottomOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.core.widget.NestedScrollView>